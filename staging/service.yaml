apiVersion: v1
kind: Service
metadata:
  name: microservice-app-service
  namespace: staging
spec:
  type: LoadBalancer
  selector:
    app: microservice-app
  ports:
  - port: 80
    targetPort: 3000